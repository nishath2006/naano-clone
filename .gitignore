# Hook runtime state (per-session counters, error log). The logs themselves in
# .agent-logs/ are intentionally NOT ignored; they ship with the repo.
.claude/hooks/state/
.claude/settings.local.json
node_modules/
